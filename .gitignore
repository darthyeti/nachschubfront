node_modules/
tests/output/
.DS_Store

# Update packages dropped into the working tree; the unpacked docs are what counts.
*.zip

# Throwaway scripts from a working session; they never belong in a commit.
*-tmp.*

# Update packages unpack a copy of their docs into the root; docs/ is where they
# are read from, so the root copies stay out of the repository.
/ART-kapsel.md
/UPDATE.md
/ART-update-v2.md
/M4d-feinschliff-sprint2.md
